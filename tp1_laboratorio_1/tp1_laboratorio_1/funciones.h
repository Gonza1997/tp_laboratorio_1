/** \brief Realiza la operación de suma
 *
 * \param El primer número a sumar
 * \param El segundo número a sumar
 * \return La suma de ambos números
 *
 */
float sumar(float, float);

/** \brief Realiza la operación de resta
 *
 * \param El primer número a restar
 * \param El segundo número a restar
 * \return La resta de ambos números
 *
 */
float restar(float, float);

/** \brief Realiza la operación de división
 *
 * \param El dividendo
 * \param El divisor
 * \return La division de ambos números
 *
 */
float dividir(float, float);

/** \brief Realiza la operación de multiplicación
 *
 * \param El primer número a multiplicar
 * \param El segundo número a multiplicar
 * \return El producto de ambos números
 *
 */
float multiplicar(float, float);

/** \brief Realiza el factoreo de un número
 *
 * \param El número a factorear
 * \return El resultado del factoreo
 *
 */
int factorialA(float);
